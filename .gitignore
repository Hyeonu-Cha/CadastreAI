.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Build / tooling
dist/
build/
.ruff_cache/
.pytest_cache/

# Data (large / gitignored)
data/raw/
data/processed/*
!data/processed/.gitkeep
data/cache/
data/training/

# Logs / scratch
*.log
.tmp_pytest/

# Training artifacts (multi-GB checkpoints + fine-tuned weights live
# outside the repo — models/bge-au-housing-v1 is rehydrated from HF or
# the upstream training run, not committed)
checkpoints/
models/

# Broken eval outputs (kept locally for debugging, never published)
*.broken.json
**/*.broken.*
